// Single source of truth for the database shape.
// Auth tables (users, sessions, etc.) land here in the follow-up auth task.
// Export new tables from this file so `db.query.<table>` and drizzle-kit
// migrations pick them up automatically.

export const schema = {};
