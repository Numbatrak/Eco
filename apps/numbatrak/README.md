# numbatrak

Placeholder workspace. Numbatrak is an existing product being migrated into this
monorepo; the actual migration (source, build config, dependencies) lands in a
later task. This folder exists only so the workspace recognizes it as a package.
